<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hc.modules.member.mapper.MemberMapper">

    <resultMap id="resultMap" type="com.hc.modules.member.entity.MemberEntity">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="birth" property="birth"/>
        <result column="guarder" property="guarder"/>
        <result column="guarder_phone" property="guarderPhone"/>
        <result column="address" property="address"/>
        <result column="create_time" property="createTime"/>
        <result column="status" property="status"/>
    </resultMap>

    <select id="getMemberList" resultMap="resultMap">
          SELECT a.* FROM member a RIGHT JOIN role_user b
          ON a.user_id = b.id WHERE b.username = #{username}
    </select>

</mapper>
